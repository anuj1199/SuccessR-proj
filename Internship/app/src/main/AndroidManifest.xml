<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vinu.internship">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivityJob"></activity>
        <activity android:name=".uploadjob" />
        <activity android:name=".uploadnotification" />
        <activity android:name=".employeer_success" />
        <activity android:name=".InternSuccess" />
        <activity android:name=".employeersignup" />
        <activity android:name=".employeesignup" />
        <activity android:name=".internsignup" />
        <activity android:name=".employeerlogin" />
        <activity android:name=".employeelogin" />
        <activity android:name=".Internlogin" />
        <activity android:name=".OnBoardingScreen" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>